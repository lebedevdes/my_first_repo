"""Модуль с функцией возвредения в квадрат"""
from typing import Number


def square(x: Number) -> Number:
    """
    Возводит в квадрат x

    Args:
        x: входное число
    Returns:
        результат возведения в квадрат
    """
    return x*x

